var test = require('tape');
var hmu = require('.');

test('input and options', function(t) {
  var foo = function foo(output, input, options) {
    return new Promise(function(resolve) {

    });
  };
});
