import test from 'ava';
import common from '../lib';

test('integer', t => {
  console.log(common);
  t.pass();
});

test('string', t => {
  t.pass();
});

test('whitespace', t => {
  t.pass();
});

test('arithmetic', t => {
  t.pass();
});
